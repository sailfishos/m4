diff -urN m4-1.4.16/lib/stdio.in.h m4-1.4.16-aarch64/lib/stdio.in.h
--- m4-1.4.16/lib/stdio.in.h	2011-03-01 17:39:29.000000000 +0100
+++ m4-1.4.16-aarch64/lib/stdio.in.h	2012-10-30 19:15:07.468253665 +0100
@@ -161,8 +161,9 @@
 /* It is very rare that the developer ever has full control of stdin,
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
-#undef gets
+#ifdef HAS_RAW_DECL_GETS
 _GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
+#endif
 
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
